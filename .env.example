# Server-only. Never prefix this with VITE_ or expose it to the browser bundle.
OPENAI_API_KEY=your_server_only_key_here

# Required when content-admin is reachable from another device.
CONTENT_ADMIN_DEV_PASSWORD=choose_a_shared_dev_password
